## Tasks

- [x] Create `src/pages/arknights-endfield/components/weaponSort.ts` with `sortWeaponsForDisplay`
- [x] Apply sort in `OperatorCard.tsx` — replace raw `equippableWeapons` with sorted version
- [x] Add unit test for `sortWeaponsForDisplay` (mixed rarities, same-rarity alpha order)
- [x] Verify equip dropdown and preference picker render in correct order (manual or existing test)
